apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml

configMapGenerator:
  - name: dt26-ambulance-webapi-mongodb-init
    files:
      - params/init-db.js
  - name: dt26-ambulance-webapi-config
    literals:
      - database=dt26-ambulance
      - collection=ambulance

patches:
- path: patches/webapi.deployment.yaml
  target:
    group: apps
    version: v1
    kind: Deployment
    name: dt26-ambulance-webapi
